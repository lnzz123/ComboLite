<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name=".receiver.StaticReceiver"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver,Instantiatable">

            <intent-filter>
                <action android:name="com.combo.plugin.example.action.STATIC_BROADCAST" />

                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_PRESENT" />

                <action android:name="android.bluetooth.adapter.action.STATE_CHANGED" />
                <action android:name="android.intent.action.AIRPLANE_MODE" />
                <action android:name="android.intent.action.HEADSET_PLUG" />

                <action android:name="android.intent.action.LOCALE_CHANGED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.DATE_CHANGED" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>

        </receiver>
        <!-- 插件配置元数据 -->
        <meta-data
            android:name="plugin.id"
            android:value="example" />
        <meta-data
            android:name="plugin.version"
            android:value="1.0.1" />
        <meta-data
            android:name="plugin.description"
            android:value="这是一个示例插件，支持中文。" />
        <meta-data
            android:name="plugin.entryClass"
            android:value="com.combo.plugin.sample.example.PluginEntryClass" />
    </application>

</manifest>