<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:name=".HostApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposePluginSample" >
        <service android:name=".HostService1" />
        <service android:name=".HostService2" />
        <service android:name=".HostService3" />
        <service android:name=".HostService4" />
        <service android:name=".HostService5" />
        <service android:name=".HostService6" />
        <service android:name=".HostService7" />
        <service android:name=".HostService8" />
        <service android:name=".HostService9" />
        <service android:name=".HostService10" />
        <activity
            android:name=".HostActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ComposePluginSample" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>